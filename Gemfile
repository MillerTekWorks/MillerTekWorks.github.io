# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"

# Gemfile
source "https://rubygems.org"

# Add the GitHub Pages gem, which includes Jekyll and other dependencies
gem "github-pages", group: :jekyll_plugins
gem 'wdm', '>= 0.1.0' if Gem.win_platform?